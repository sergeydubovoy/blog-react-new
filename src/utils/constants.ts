export const HEADING: string = "МОЙ БЛОГ";

export const TEXT_LENGTH: {
  titleMax: number;
  textMax: number;
  textZero: number;
} = {
  titleMax: 100,
  textMax: 200,
  textZero: 0,
};

export const INPUT_NAME: {
  titleNameForm: string;
  textNameForm: string;
  titleNamePopup: string;
  textNamePopup: string;
} = {
  titleNameForm: "title",
  textNameForm: "text",
  titleNamePopup: "title",
  textNamePopup: "text",
};

export const BUTTON_TEXT: { publish: string; save: string } = {
  publish: "ОПУБЛИКОВАТЬ",
  save: "СОХРАНИТЬ",
};

export const PLACEHOLDERS: { title: string; text: string } = {
  title: "Напишите заголовок поста",
  text: "Напишите текст поста",
};